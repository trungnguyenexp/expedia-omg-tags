if(b["entity.checkout.cartTotal.amount"])
{
    b["bingBatEv"] = Math.round(b["entity.checkout.cartTotal.amount"]);
}

/** old
 if(utag_data["entity.checkout.cartTotal.amount"])
 {
    utag_data["bingBatEv"] = Math.round(utag_data["entity.checkout.cartTotal.amount"]);
 }
 **/