if (b['entity.checkout.coupon.amount'] != undefined)
{
    b['checkout.coupon.amount'] = b['entity.checkout.coupon.amount'];
}

/** old
 if (utag_data['entity.checkout.coupon.amount'] != undefined)
 {
   utag_data['checkout.coupon.amount'] = utag_data['entity.checkout.coupon.amount'];
 }
 **/