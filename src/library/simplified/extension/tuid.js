if (b['context.user.tuid'] != undefined)
{
    b['tuid'] = b['context.user.tuid'];
}

/** old
 if (utag_data['context.user.tuid'] != undefined)
 {
   utag_data['tuid'] = utag_data['context.user.tuid'];
 }
 **/