// Alias of GBV
b['phgTotalPrice'] = '';
if (b['checkout.cartTotal.amount'])
{
    b['phgTotalPrice'] = b['checkout.cartTotal.amount'];
}
/** old
 // Alias of GBV
 utag_data['phgTotalPrice'] = '';
 if (utag_data['checkout.cartTotal.amount'])
 {
     utag_data['phgTotalPrice'] = utag_data['checkout.cartTotal.amount'];
 }
 **/