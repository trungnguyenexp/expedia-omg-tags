if (b['context.site.siteName'] != undefined)
{
    b['siteName'] = b['context.site.siteName'];
}

/** old
 if (utag_data['context.site.siteName'] != undefined)
 {
   utag_data['siteName'] = utag_data['context.site.siteName'];
 }
 **/