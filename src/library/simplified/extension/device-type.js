if (b['device_type'] != undefined)
{
    b['device.device.type'] = b['device_type'];
}

/** old
 if (utag_data['device_type'] != undefined)
 {
   utag_data['device.device.type'] = utag_data['device_type'];
 }
 **/