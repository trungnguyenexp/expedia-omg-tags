// TODO: Merge with device-os.js
if (b['device_details'] != undefined)
{
    b['device.device.details'] = b['device_details'];
}

/** old
 if (utag_data['device_details'] != undefined)
 {
   utag_data['device.device.details'] = utag_data['device_details'];
 }
 **/