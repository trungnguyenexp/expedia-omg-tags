if(utag.isCarCO() && b.entity.tripDetails.totalPrice.amount != undefined){
    b['ItinPageAmount'] = b['entity.tripDetails.totalPrice.amount'];
}

/** old
 if(utag.isCarCO() && utag_data.entity.tripDetails.totalPrice.amount != undefined){
  utag_data['ItinPageAmount'] = utag_data['entity.tripDetails.totalPrice.amount'];
}
 **/